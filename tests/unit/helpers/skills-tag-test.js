
import { skillsTag } from 'sarah-cv/helpers/skills-tag';
import { module, test } from 'qunit';

module('Unit | Helper | skills tag');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = skillsTag([42]);
  assert.ok(result);
});

